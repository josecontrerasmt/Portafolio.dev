<nav id="header-nav" class="fixed w-full z-50 h-16 top-0">
  <div
    class="flex h-full justify-between items-center overflow-hidden max-w-2xl lg:max-w-4xl xl:max-w-6xl mx-auto px-4 md:px-0"
  >
    <img
      class="w-8 md:w-10 object-cover bg-black dark:bg-transparent rounded-full"
      src="/Logo.png"
      alt="Mi Logo"
    />

    <div class="hidden md:flex space-x-8 font-semibold">
      <a
        href="#inicio"
        class="text-black dark:text-white dark:hover:text-green-400 transition-colors"
        >Inicio</a
      >
      <a
        href="#proyectos"
        class="text-black dark:text-white dark:hover:text-green-400 transition-colors"
        >Proyectos</a
      >
      <a
        href="#habilidades"
        class="text-black dark:text-white dark:hover:text-green-400 transition-colors"
        >Habilidades</a
      >
      <a
        href="#sobre-mi"
        class="text-black dark:text-white dark:hover:text-green-400 transition-colors"
        >Sobre mi</a
      >
      <a
        href="#contactame"
        class="text-black dark:text-white dark:hover:text-green-400 transition-colors"
        >Contacto</a
      >
    </div>
    <button id="themeToggle" class="text-xl cursor-pointer">
      <span class="hidden dark:inline"
        ><i class="bx bxs-sun text-white"></i></span
      >
      <span class="inline dark:hidden"
        ><i class="bx bxs-moon text-black"></i></span
      >
    </button>
    <div class="md:hidden flex items-center">
      <button
        id="menu-button"
        class="text-black dark:text-white dark:hover:text-green-400 focus:outline-none"
      >
        <svg
          class="w-6 h-6"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
    </div>
  </div>

  <div id="menu" class="hidden md:hidden">
    <div
      class="text-center px-4 py-4 space-y-5 border-2 rounded-lg bg-white border-black dark:border-white dark:bg-[#151515]"
    >
      <a
        href="#inicio"
        class="text-black dark:text-white dark:hover:text-green-400 link"
        >Inicio</a
      >
      <a
        href="#proyectos"
        class="block text-black dark:text-white dark:hover:text-green-400 link"
        >Proyectos</a
      >
      <a
        href="#habilidades"
        class="block text-black dark:text-white dark:hover:text-green-400 link"
        >Habilidades</a
      >
      <a
        href="#sobre-mi"
        class="block text-black dark:text-white dark:hover:text-green-400 link"
        >Sobre mi</a
      >
      <a
        href="#contactame"
        class="block text-black dark:text-white dark:hover:text-green-400 link"
        >Contacto</a
      >
    </div>
  </div>
</nav>

<script>
  const themeToggle = document.getElementById("themeToggle");
  themeToggle?.addEventListener("click", () => {
    document.documentElement.classList.toggle("dark");
  });

  document.addEventListener("DOMContentLoaded", () => {
    const menuButton = document.getElementById("menu-button");
    const menu = document.getElementById("menu");
    const links = document.querySelectorAll(".link");

    links.forEach((link) => {
      link.addEventListener("click", () => {
        menu?.classList.toggle("hidden");
      });
    });
    if (menuButton != null) {
      menuButton.addEventListener("click", () => {
        if (menu != null) {
          menu.classList.toggle("hidden");
        }
      });
    }
  });
</script>

<style>
  #header-nav {
    animation: blur linear both;
    animation-timeline: scroll();
    animation-range: 0 500px;
  }

  @keyframes blur {
    to {
      box-shadow: var(--nav-shadow);
      background: var(--nav-bg);
      backdrop-filter: blur(8px);
    }
  }
</style>
